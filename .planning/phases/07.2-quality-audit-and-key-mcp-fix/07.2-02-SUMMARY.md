---
phase: 07.2-quality-audit-and-key-mcp-fix
plan: 02
subsystem: auth
tags: [oauth, mcp, cli, inline-auth, readonly-key]

requires:
  - phase: 05-mcp-setup
    provides: MCP editor config writing utilities
  - phase: 02-auth
    provides: OAuth flow pattern with localhost callback server
provides:
  - Inline OAuth flow for --key mode MCP readonly key provisioning
  - Graceful MCP skip when user declines or auth fails
affects: [setup-mcp, auth, liftoff-cli]

tech-stack:
  added: []
  patterns: [inline-oauth-for-optional-features]

key-files:
  created: []
  modified:
    - packages/ts/liftoff/src/steps/setup-mcp.ts

key-decisions:
  - "Inline OAuth mirrors auth.ts pattern but only provisions readonly key (not full auth context)"
  - "shouldRun always returns true now; missing readonlyKey handled inside run() with user prompt"

patterns-established:
  - "Inline OAuth for optional features: prompt user, run OAuth inline, provision specific resource"

requirements-completed: [D-07, D-08, D-09, D-10, D-14]

duration: 1min
completed: 2026-03-31
---

# Phase 07.2 Plan 02: Inline OAuth for --key Mode MCP Summary

**Inline OAuth flow in setup-mcp.ts so --key mode users can authenticate for MCP readonly key provisioning**

## Performance

- **Duration:** 67s
- **Started:** 2026-03-31T09:21:05Z
- **Completed:** 2026-03-31T09:22:12Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments
- Removed shouldRun guards that blocked --key mode users from MCP setup entirely
- Added acquireReadonlyKey helper with full OAuth flow (browser, callback server, code exchange, key provisioning)
- Users in --key mode now get prompted to log in for MCP; declining skips gracefully
- Normal auth-flow MCP path unchanged

## Task Commits

Each task was committed atomically:

1. **Task 1: Add inline OAuth flow for --key mode MCP setup** - `95fc23e` (feat)

**Plan metadata:** [pending] (docs: complete plan)

## Files Created/Modified
- `packages/ts/liftoff/src/steps/setup-mcp.ts` - Added inline OAuth flow with acquireReadonlyKey helper, updated shouldRun to allow all users

## Decisions Made
- Inline OAuth mirrors auth.ts pattern but only provisions readonly key (not full auth context)
- shouldRun always returns true now; missing readonlyKey handled inside run() with user prompt
- Auth timeout kept at 30s matching the main auth step

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- Pre-existing TypeScript errors in frameworks/*.ts files (PackageManager undefined checks) -- out of scope, not related to this plan's changes

## Known Stubs

None - all data flows are wired and functional.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- MCP setup now works for both --key and normal auth mode users
- All Phase 07.2 plans complete

---
## Self-Check: PASSED

- [x] setup-mcp.ts exists with inline OAuth flow
- [x] SUMMARY.md created
- [x] Task 1 commit 95fc23e verified

---
*Phase: 07.2-quality-audit-and-key-mcp-fix*
*Completed: 2026-03-31*
